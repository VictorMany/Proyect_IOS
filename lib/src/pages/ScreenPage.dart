import 'package:flutter/material.dart';

class ScreenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Screen settings'),
      ),
      body: Text("Bienvenido a la configuración de la pantalla"),
    );
  }
}

import 'package:flutter/material.dart';

class ConnectionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Phone connections'),
      ),
      body: Text("Bienvenido a la configuracion de las conexiones"),
    );
  }
}
